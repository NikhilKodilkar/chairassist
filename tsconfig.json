// Solution tsconfig for `tsc -b`.
// The React app and the Vite Node config are typechecked separately
// so browser DOM types do not leak into vite.config.ts.
{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ]
}
